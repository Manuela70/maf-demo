/**
 * AnnotationNote - Componente para notas de documentación
 * No renderiza nada, solo para referencia en código
 */

const { React } = window;

export function AnnotationNote({ type, id, text }) {
  return null;
}

export default AnnotationNote;
